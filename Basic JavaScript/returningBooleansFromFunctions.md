# Returning Boolean Values from Functions

### Comparison operators:

> | greater than
< | less than
= | equal 
== | equality
!= | inequality
=== | strict equality
!== | strict inequality

## SIMPLIFIED Example

```javascript
function isLess(a, b) {
    return a < b;
}
console.log(isLess(10, 15));
```